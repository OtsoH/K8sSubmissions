[project]
name = "broadcaster"
version = "0.1.0"
description = "Forwards todo events from NATS to an external chat service"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "nats-py>=2.9.0",
]
